using UnityEngine;
using UnityEngine.XR.Interaction.Toolkit;

public class SlapDuck : MonoBehaviour
{
    // public float ducksPerSlap = 1f;
    // public float slapCooldown = 0.3f;

    // private float lastSlapTime = -999f;

    // private void OnCollisionEnter(Collision collision)
    // {
    //     if (Time.time - lastSlapTime < slapCooldown)
    //         return;


    //     Debug.Log("Slap successful");
    //     lastSlapTime = Time.time;
    //     ResourceManager.Instance.AddDucks(ducksPerSlap);
    // }
}